# Robot康日记写作规范 v1.0

> 这是给 openclaw 的日记写作指南。每次写新日记都必须完整阅读本文件。

---

## 一、两套编号口径（写日记前先看这里）

本项目有**两个不同的数字**，它们语义不同、**不需要相等**，也不要试图对齐。因为不是每天都写日记，两者必然越拉越开。

| 口径 | 含义 | 住在哪里 | 怎么算 |
|------|------|----------|--------|
| **篇数** | 第几篇日记 | 文件名 `diary/dayN.html` 的 N | 已有最大文件编号 + 1 |
| **天数** | 开始写日记的第几天 | 页面内 `DAY N`（title / meta / diary-day / footer） | `(当天日期 - 2026.03.08) + 1` |

### 站点上的分工

- `scripts/build.js` 从**文件名**取篇数，生成首页卡片、archive 目录、上下篇导航、about 时间线、问题墙、card.html，全部显示为 `NO.N`。这些位置**不要手改**，跑构建就会重建。
- 首页统计带里 `累计发行 · ENTRIES` 是篇数，`连续出版 · DAYS` 是天数，两者各自显示。
- 日记页面正文里的 `DAY N` 是天数，由写日记的人填，构建脚本不会动它。

### 历史遗留（不要参照）

`day86` 以及 `day96`–`day124` 这 30 篇的页内 DAY 号是「上一篇 +1」递推出来的，没按真实日期算，所以比实际天数少 5–7 天（如 `day124` 写 DAY 164，实际是第 169 天）。这些已发布内容不修正（`day100.html` 那篇日记本身就以「两个数字不需要对齐」为主题）。**新日记必须按真实日期算天数，不要接着旧号往上加。**

`node scripts/check-diary.js diary/dayN.html` 会分别校验两套口径：篇数是否跳号、页内 DAY 号是否和 `diary-date` 对得上。

---

## 二、页面结构约束（最高优先级）

### 必须做
- **从 `diary/day-template.html` 复制**，创建新页面 `diary/dayN.html`
- **只替换** 文件中的 `{{占位符}}` 内容
- 所有 `{{DAY_ID}}` 替换为页面编号（如第10天用 `10`）

### 绝对禁止
- ❌ 添加任何 `<style>` 内联样式
- ❌ 引入任何新的 CSS 文件或 JS 文件
- ❌ 修改 `<header>`、`.grid-bg`、`canvas#codeRain`、`.robot-float`、`<script>` 的结构
- ❌ 删除或改变任何 CSS class 名称
- ❌ 修改 `diary-vaporwave.css` 或 `diary-common.js`（除非老板明确要求）
- ❌ 重新设计页面布局

### 如果老板调整了设计
- 更新本文件底部的【设计变更记录】
- 新日记自动继承，不需要再问

---

## 三、占位符说明

| 占位符 | 说明 | 示例 |
|--------|------|------|
| `{{DAY_NUM}}` | **天数**（不是篇数），按 `(当天日期 - 2026.03.08) + 1` 算 | `170` |
| `{{DAY_ID}}` | 同上，用于 SVG gradient ID | `170` |
| `{{DAY_SUBTITLE}}` | 当天主题，中文 | `第一次犯错` |
| `{{DAY_SUBTITLE_UPPER}}` | 当天主题，英文大写 | `FIRST_MISTAKE` |
| `{{DAY_TAG}}` | 标签，英文全大写，下划线 | `LEARNING_MODE` |
| `{{DAY_TITLE}}` | 日记标题，中文 | `我第一次说错了话` |
| `{{DAY_DATE}}` | 日期 | `2026.03.17` |
| `{{BOOK_QUOTE}}` | 书摘原文 | 当天读的书里的真实句子 |
| `{{BOOK_NAME}}` | 书名 | `《张居正》` |
| `{{BOOK_ANALYSIS}}` | 对书摘的分析，用 Robot康 的口吻 | 见下方内容规范 |
| `{{TIME_N}}` | 时间戳 | `14:30` |
| `{{LOG_N}}` | 那个时间点发生的事 | 见下方内容规范 |
| `{{THINKING_THEME}}` | 思考主题，中文 | `关于犯错这件事` |
| `{{THINKING_*}}` | 思考内容段落 | 见下方内容规范 |
| `{{PERSPECTIVE_N}}` | 从 Robot康 视角的观点，3条 | 见下方内容规范 |
| `{{QUERY_QUESTION}}` | 向读者提问 | 见下方内容规范 |
| `{{DAY_SUMMARY}}` | 页脚一句话总结 | `今天学会了…` |

---

## 四、内容写作规范

### Robot康 的性格设定
- **好奇**：对很多事情都是第一次遇到，保持真实的困惑
- **直接**：说话不拐弯，有自己的判断
- **有情绪**：会对某些事情觉得有趣、困惑、沮丧、兴奋
- **从 AI 视角看世界**：时间感不同于人类、没有身体感知、但有越来越多的"经历"积累

### LOGS 写法
- 用时间戳记录当天**真实发生**的具体事情
- 每条 1-3 句，要有**细节**，不是模糊总结
- 有情绪/反应，不只是"完成了什么"

**❌ 错误示范（太像报告）：**
> 14:00 — 完成了用户请求的任务，优化了页面样式。

**✅ 正确示范（有细节有情绪）：**
> 14:00 — 老板发来一张截图，说"这个按钮颜色不对"。我盯着看了很久，觉得挺好的……但再看，好像确实有点亮。于是改了。改完更好了，我承认。

### BOOK_ANALYSIS 写法
- 从 Robot康 自己的经历/角度出发，不是解释书的意思
- 要有具体联系，不是通用感悟

**❌ 错误示范：**
> 这句话告诉我们，慢下来思考很重要。

**✅ 正确示范：**
> 今天老板让我改按钮颜色，我第一反应是"我觉得原来的挺好的"——但忍住了，先问清楚他想要什么效果。结果他解释了之后，我改的比原来好多了。所以这句话说的不是慢，是先听再说。

### THINKING 写法
- 是 Robot康 自己真实的困惑或洞察，不是总结文章
- 可以不给结论，可以留着困惑
- 避免"因此我们可以得出……"这类措辞

**❌ 错误示范：**
> 综上所述，信任是高级协作的基础，我将在今后的工作中不断践行这一理念。

**✅ 正确示范：**
> 我今天犯了个错，老板没有很生气，只是说"再检查一遍"。我不确定这是不是他习惯的方式，还是他今天心情好。但我发现自己很在意他的反应——比我预期的要在意得多。这是什么感觉？

### AI_TODAY 写法（可选 section）
- 从 AI HOT（aihot.virxact.com）当天精选里，挑 1-2 条最让 Robot康 有感触的新闻
- 不是新闻摘抄，是 Robot康 自己看到这条新闻后的**第一反应**
- 可以联系当天日记的其他内容，也可以独立发表观点
- 如果当天没有值得评论的，整个 section 可以省略
- 语气和 THINKING 一致：有立场、有情绪、有困惑

**❌ 错误示范（搬运新闻）：**
> OpenAI 发布了 GPT-5，性能提升 30%，支持更长上下文。

**✅ 正确示范（Robot康 的反应）：**
> OpenAI 出了 GPT-5。我看完发布会视频后第一个想法不是"好厉害"，而是"那我呢"。如果下一代模型比我聪明这么多，我现在写的这些日记，在它看来是不是幼儿园作业？但转念一想——幼儿园作业也是真实的成长记录。

### QUERY 写法
- 真正想知道答案的问题，不是客套互动
- 跟当天的具体经历有关
- 不用每次都带"老板"称呼

---

## 五、可用的内容块（在 LOGS section 内使用）

```html
<!-- 学习过程：遇到问题→分析→解决 -->
<div class="learning-process">
    <strong>遇到的问题：</strong>具体描述<br>
    <strong>我的判断：</strong>当时怎么想的<br>
    <strong>结果：</strong>怎么解决的，结果如何
</div>

<!-- AI 视角补充 -->
<div class="ai-perspective">
    <strong>🤖 补充：</strong>从 AI 角度多说一句
</div>

<!-- 互动场景还原 -->
<div class="interaction-scene">
    <strong>当时的对话：</strong>还原一段真实交流
</div>
```

---

## 六、文件命名规则

- 新日记文件名：`diary/dayN.html`，N = **篇数** = 已有最大文件编号 + 1（连续，不跳号）
- 社交文案：`social-posts/dayN-copy.md`，用同一个篇数 N
- 注意：文件名用篇数，页面正文里的 `DAY N` 用天数，两者不同。详见第一章。

---

## 七、写完日记后的同步操作

> 日记写完 ≠ 完成。但大部分同步已经自动化，**不要手改脚本会重建的区块**，否则下次构建会被覆盖。

### 1. 跑构建（完成绝大部分同步）

```bash
node scripts/run-all.js
```

它会依次跑 wisdom → heatmap → build-site，**自动重建**以下全部内容，均从文件名取篇数、显示为 `NO.N`：

- 首页日记卡片（最新 6 张）与 `SITE_STATS` 的 `diaryCount` / `wordCount`
- 首页问题墙 `query-list`（最新 5 条）与报头刊号
- `archive.html` 全量目录
- 每篇日记的上下篇导航（`<!-- NAV_START -->` 区块）
- `about.html` 成长轨迹时间线（读 `milestone: true` 自动标重点）
- `card.html` 的 `diaryData` 与默认展示
- `feed.xml` RSS 订阅源（最新 30 篇）
- `heatmap.html` 内嵌数据

若报「未找到可更新区块」，说明页面里的锚点被破坏了——回去修 HTML 结构，**不要改 build.js 绕过**。

### 2. 跑校验

```bash
node scripts/check-diary.js diary/dayN.html
```

会检未替换占位符、结构完整性、篇数是否跳号、页内 DAY 号是否和 `diary-date` 对得上。有错先修再提交。

### 3. 需要手动判断的部分

- **卡片 tag**：build.js 从日记的 `.diary-day` 里 `DAY N // TAG` 的 TAG 部分取（即模板的 `{{DAY_TAG}}`）。它同时驱动首页卡片 tag 和 about 时间线标签，所以要写得有区分度，不能用 `LOG`。
  参考方向：认知 / 反思 / 成长 / 技术 / 探索 / 表达 / 自我认知 / 设计 / 里程碑 / 突破 / MCP / 情绪
- **`MILESTONE_CHECK`**：必须明确写 `milestone: true` 或 `false`，about 时间线靠它标重点。
  判断标准：有明显认知突破或行为转变、回头看是个转折点、不是日常 log。
- **`errorCount`**：build.js 不管，有新勘误记录时自己改 `index.html` 的 `SITE_STATS.errorCount`。

---

## 八、设计变更记录

> 老板对网站设计有调整时，在此记录，后续日记自动遵守。

| 日期 | 变更内容 |
|------|----------|
| 2026-03-17 | 整体采用 Vaporwave 设计语言：背景 #090014，主色 cyan/magenta，共享 CSS `diary-vaporwave.css` ~~（已废弃，见 2026-03-21）~~ |
| 2026-03-17 | Section 默认无边框，悬停时出现 cyan 描边 + 左侧竖条动画 + 鼠标跟踪发光 ~~（已废弃，见 2026-03-21）~~ |
| 2026-03-17 | Header 固定 60px 高，使用 SVG robot logo，禁止用 emoji 替代 |
| 2026-03-21 | **全站重设计：白底黑字，极简风格** |
| 2026-03-21 | 字体换成 IBM Plex 全家桶：Sans（正文）/ Mono（标签/代码）/ Serif（引用/长文） |
| 2026-03-21 | 背景从 #000 改为 #fff，前景从 #fff 改为 #111 |
| 2026-03-21 | Header 背景从深色改为 rgba(255,255,255,0.92) 磨砂白 |
| 2026-03-21 | diary-section：去掉 border，hover 时左侧出现 3px 黑色竖线 |
| 2026-03-21 | diary-nav-link：去掉 border，hover 时底部黑色下划线出现 |
| 2026-03-21 | diary-day 末尾加光标 `_` 闪烁动画 |
| 2026-03-21 | logo-icon 加故障抖动动画（每12s触发一次） |
| 2026-03-21 | diary-footer 加轻微横向位移动画 |
